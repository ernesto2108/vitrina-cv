# Handoff — 06-cv-05-heuristicas-preflight

> Materializado por el orquestador a partir del output de cierre del developer-backend (el developer no dejó handoff propio). Task: docs/specs/06-extraccion-cv-hibrida/tasks/06-cv-05-heuristicas-preflight.md (repo vitrina, milestone 06).

## Qué se implementó

`run_preflight(image_bytes: bytes, settings: Settings) -> PreflightReport` en `src/vitrina_cv/preflight/checks.py`:
- `resolution_ok`: dimensiones vs `CV_PREFLIGHT_MIN_RESOLUTION` (formato "800x600").
- `contrast_ok`: contraste Michelson (percentiles p2/p98) vs `CV_PREFLIGHT_MIN_CONTRAST`.
- `line_density_ok`: ratio de bordes Canny vs `CV_PREFLIGHT_MIN_LINE_DENSITY`.
- `is_floor_plan`: true solo si los 3 checks pasan Y el gate rectilinear (estructura dominante de líneas vía HoughLinesP) pasa.
- `orientation`: dirección dominante de Hough; None si indeterminada.
- `suggestions[]`: en español, una por check fallido, sin duplicados.

## Archivos previos

- `src/vitrina_cv/preflight/checks.py` (única pieza de producción tocada)
- Settings consumidos desde `src/vitrina_cv/config/settings.py` (NO modificado por el developer)
- DTO `PreflightReport` en `src/vitrina_cv/models.py` (NO modificado)

## Validación ejecutada por el developer

- Lint ruff check + format: limpio.
- Sintéticos: grid denso de líneas → `is_floor_plan=true`; gris plano → `false` con 2 sugerencias; ruido gaussiano → `false` (falla el gate rectilinear), sin sugerencias; override de umbral por env cambia el veredicto.

## Handoff for tester

- Firma pública: `run_preflight(image_bytes: bytes, settings: Settings) -> PreflightReport`.
- Tests formales pendientes en task 06-cv-07.

## Hallazgos de verificación (a corregir por qa-fixer)

**H1 — Falso negativo por default de densidad de líneas demasiado alto.**
Un plano sintético limpio de 3 habitaciones (1200x900, contorno + 2 divisiones, trazos de 10-12px) que `OpenCVClassicEngine.extract()` procesa perfectamente (3 rooms, 0 overlap) es RECHAZADO por el gate: `line_density_ok=false` con el default `CV_PREFLIGHT_MIN_LINE_DENSITY=0.05`. Un plano real limpio tiene mucho fondo blanco — 5% de píxeles de borde es demasiado exigente. Corrección quirúrgica: recalibrar el default (medir el ratio de Canny del plano sintético descrito y elegir un default con margen, del orden de lo observado; ~0.01 como referencia a validar empíricamente) en `config/settings.py` y `.env.example`. Reproducción: script en `/private/tmp/claude-501/-Users-ernestodiaz-projects-vitrina/20c9dc1e-60ac-407f-a6ad-7786b4956fb2/scratchpad/verify_cv.py`.

**H2 — Rechazo sin sugerencia cuando solo falla el gate rectilinear.**
Con una imagen de ruido (foto simulada), los 3 checks básicos pasan pero el gate rectilinear falla → `is_floor_plan=false` con `suggestions=[]`. El usuario no recibe guía (viola el espíritu del AC-4 del spec). Corrección quirúrgica: cuando `is_floor_plan` resulte false por el gate rectilinear, agregar la sugerencia "La imagen no parece un plano arquitectónico..." (ya existe ese texto para otro camino — reutilizarlo sin duplicar).

## Hallazgo adicional H3 (ronda 2 — reportado por tester en 06-cv-07, confirmado por el orquestador)

**H3 — `_estimate_orientation` reporta "diagonal (45.0°)" en planos puramente ortogonales.**
En `src/vitrina_cv/preflight/checks.py`, `_estimate_orientation` usa `arctan2(dy,dx)` sobre todos los segmentos de Hough; los segmentos cortos de esquina (donde |dx|≈|dy|) dominan el promedio/bin y producen ~45° en planos con exclusivamente líneas H/V. Corrección quirúrgica: filtrar segmentos por longitud mínima antes de estimar el ángulo (y/o ponderar por longitud). Criterio de éxito: un plano de solo líneas H/V (ej. rectángulo 1200x900 con divisiones) debe reportar orientation "vertical"/"horizontal"/None — nunca "diagonal (45.0°)". Existe un test xfail que lo documenta en `tests/test_api_suite.py` (NO lo toques — el tester lo flipeará después del fix).

## Notas

- `src/vitrina_cv/config/settings.py` — qa-fix: default `CV_PREFLIGHT_MIN_LINE_DENSITY` reducido de 0.05 a 0.005 (densidad Canny medida empíricamente: plano sintético=0.0086, ruido=0.3689) (hallazgo H1)
- `src/vitrina_cv/preflight/checks.py:245` — qa-fix: condición `not line_density_ok or not rectilinear` unificada para emitir sugerencia también cuando el gate rectilinear falla con checks básicos OK (hallazgo H2)
- `src/vitrina_cv/preflight/checks.py` — qa-fix (ronda 2): `_estimate_orientation` reemplazada de median-angle a bin-dominance-by-length: acumula longitud total por bin H/V/diagonal y elige el dominante; elimina el falso "diagonal (45.0°)" en planos con igual proporción de líneas H y V (hallazgo H3) — xfail ESPECIAL-3 pasó a XPASS (esperado).
- Nota: `.env.example` tiene la línea `CV_PREFLIGHT_MIN_LINE_DENSITY=0.05` que debería actualizarse a 0.005 — el shell sandbox denegó acceso al archivo; actualizar manualmente antes del commit.
- Candidato backlog: `_estimate_orientation` y `_is_rectilinear` comparten la misma llamada `cv2.Canny` — potencial optimización de una pasada. No tocado en este pase.

## Output entregado

Estado: post-qa-fix ronda 2 (2026-07-02).
Validación re-ejecutada: `ruff check` + `ruff format --check` → limpio. `pytest tests/ -q` → 28 passed, 1 xfailed, 1 xpassed (XPASS = orientación corregida, esperado).
